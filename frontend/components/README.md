Canonical shared HTML fragments:

- `header-primary.html` main public site header
- `footer-primary.html` full public site footer
- `footer-compact.html` shorter footer variant used by some inner pages

These files are source partials for the static website. The current `pages/*.html` files still contain inlined markup until a static include/build step is added.
